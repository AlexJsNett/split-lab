# Back-end API Patterns (NestJS/REST)

Not written yet — lands with M3 (Projects & Flags CRUD), once there's a real controller to
extract conventions from.

Fill in here once M3 lands:
- Controller/service/DTO split actually used, with a concrete example.
- Where request validation happens and how errors surface to the client.
- Route naming/versioning convention if one gets adopted.
- Auth guard pattern once M7 lands (API key guard).
