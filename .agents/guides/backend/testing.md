# Back-end Testing (Jest)

Not written yet — lands with M8 (Tests), though the assignment/bucketing logic from M4 is a
good candidate to unit-test as soon as it exists rather than waiting for M8 specifically.

Fill in here once M8 lands:
- What gets unit-tested vs e2e-tested in this codebase specifically.
- How Nest's testing module (`Test.createTestingModule`) is used here — mocked providers vs
  a real test DB.
- Command to run a single test file.
