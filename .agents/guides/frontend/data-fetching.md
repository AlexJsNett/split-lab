# Front-end Data Fetching

Not written yet — lands with M6 (Frontend wired to the real API).

Fill in here once M6 lands:
- Read pattern (plain `fetch`, SWR, React Query — whichever gets adopted) and where the API
  base URL is configured.
- Write pattern and how the UI re-fetches/updates after a mutation.
- Error/loading state convention used across the dashboard screens.
