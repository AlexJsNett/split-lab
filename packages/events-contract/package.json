{
  "name": "@split-lab/events-contract",
  "version": "0.0.1",
  "private": true,
  "description": "Inter-service message contract shared by @split-lab/api (producer) and @split-lab/event-processor (consumer) over RabbitMQ. Nothing else — no DB schema, no framework code.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
